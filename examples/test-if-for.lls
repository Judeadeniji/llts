@const $std = @import("std/index");

pub @func main() {
    $arr: []int = [10, 20, 30];
    $i: int = 0;
    $x = @if (i < 3) { break arr[i]; } @else { break null; };
    @for (x) |v| {
        std.debug.printLn("{i}", v);
        i = i + 1;
    }
}
