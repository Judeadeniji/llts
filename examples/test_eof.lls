@const $std = @import("std/index");

pub @func main() {
    $line = std.io.readLine();
    std.debug.printLn("type: {s}", @typeOf(line));
    @if (line == null) {
        std.debug.printLn("is null");
    }
}
main();
